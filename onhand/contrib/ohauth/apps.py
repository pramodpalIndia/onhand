from django.apps import AppConfig

# from django.utils.translation import ugettext_lazy as _


class OnHandAuthConfig(AppConfig):
    name = 'onhand.contrib.ohauth'
    verbose_name = ("Onhand Authentication and Authorization")
